export function initGeneralizedStudyEvents(generalizedStudy) {
  const container = document.getElementById("generalized-study-container");
  if (!container) return;

  // O floating button já é criado e inicializado em _setupBindings
  // Este arquivo pode ser expandido para adicionar mais eventos se necessário
}
