/**
 * Weekly Goals Events
 * Inicializa eventos relacionados às metas semanais
 */

export function initWeeklyGoalsEvents(weeklyGoalsController) {
  // O controller já cuida dos eventos no método attachEvents()
  // Este arquivo pode ser expandido para eventos adicionais se necessário
}
